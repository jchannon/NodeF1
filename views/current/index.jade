extends ../layout

block title
	title Current Driver Standings

block content
		h1 Node F1
		h2 Current Driver Standings
		h3= RaceData.season
		ul  
			each item in RaceData.StandingsLists[0].DriverStandings
				li #{item.position}.  #{item.Driver.givenName} #{item.Driver.familyName} 
					b #{item.points} 
					| points